package controller.tree.actions;

import gui.editor.Editor;

import java.awt.event.ActionEvent;
import java.awt.event.KeyEvent;

import javax.swing.KeyStroke;

@SuppressWarnings("serial")
public class SaveWorkSpaceAction extends AbstractTreeAction{
	public SaveWorkSpaceAction() {
		putValue(ACCELERATOR_KEY, KeyStroke.getKeyStroke(KeyEvent.VK_S, ActionEvent.CTRL_MASK));
		putValue(SMALL_ICON, loadIcon("Images/Icons/save.png"));
		putValue(NAME, Editor.getInstance().getResourceBundle().getString("btnSnimi"));
		putValue(SHORT_DESCRIPTION, Editor.getInstance().getResourceBundle().getString("btnSnimi"));
	}
	public void actionPerformed(ActionEvent arg0) {
		
	}

}
